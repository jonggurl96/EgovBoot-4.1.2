<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.ablyr.mapper.SampleMapper">

    <select id="selectOne" resultType="int">
        SELECT 1
    </select>

    <select id="selectSampleAdd" parameterType="sampleDTO" resultType="sampleVO">
        SELECT ${a} + ${b} AS c
    </select>
</mapper>